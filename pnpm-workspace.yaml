# pnpm 10+/11 reads settings from here (not package.json's "pnpm" field).
# Approve native build scripts so argon2 (native binding) + esbuild build on install.
onlyBuiltDependencies:
  - argon2
  - esbuild
